// Layout configuration
export const layoutConfig = {
  // Chat content max width - can be set to pixels (e.g., "750px") or percentage (e.g., "100%")
  chatContentMaxWidth: "750px", // Options: "750px", "100%", "80%", etc.
  
  // Minimum panel widths in pixels
  minPanelWidth: 325, // Minimum width for main and aside panels
}